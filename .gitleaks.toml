title = "Yaler"

[extend]
useDefault = true

[allowlist]
description = "Documented placeholders, not live credentials"
paths = [
    '''(^|/)\.env\.example$''',
    '''(^|/)docs/BRAND\.md$''',
]
regexTarget = "match"
regexes = [
    '''your_gemini_api_key_here''',
    '''GEMINI_API_KEY=$''',
]
