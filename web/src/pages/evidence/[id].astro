---
import Layout from '../../layouts/Layout.astro';
import EvidenceForm from '../../components/islands/EvidenceForm';

const { id } = Astro.params;
---

<Layout title={`Evidence Submission ${id} — Yaler`}>
  <div class="py-6">
    {id && <EvidenceForm client:load missionId={id} />}
  </div>
</Layout>
